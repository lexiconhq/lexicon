export const AVATAR_ICON_SIZES = {
  l: 96,
  m: 52,
  s: 40,
  xs: 28,
};

export const AVATAR_IMAGE_SIZES = {
  xl: 450,
  l: 150,
  m: 100,
  s: 50,
};

export const AVATAR_LETTER_SIZES = {
  l: 72,
  m: 36,
  s: 28,
  xs: 16,
};

export const EMOJI_SIZES = {
  l: 64,
  m: 42,
  s: 24,
  xs: 16,
};

export type AVATAR_ICON_SIZE_VARIANTS = keyof typeof AVATAR_ICON_SIZES;
export type AVATAR_IMAGE_VARIANTS = keyof typeof AVATAR_IMAGE_SIZES;
export type EMOJI_SIZES_VARIANTS = keyof typeof EMOJI_SIZES;
